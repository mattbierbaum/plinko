local util = require('plinko.util')
local vector = require('plinko.vector')

local PatternBuilder = util.class()
function PatternBuilder:init()
end

function PatternBuilder:cell(cell)
end

function PatternBuilder:scale(ratio)
end

return {
}
